Pseudo code 

mergesort(A):
mergesort(A, 0,len(A)-1)

mergesort(A, aux, lo, hi):
if (hi - lo <= 1)return
mid = (lo + hi) / 2
mergesort(A, lo, mid)
mergesort(A, mid+1,hi)
C = merge(A[lo:mid], A[mid+1:hi])
copy elements from C back intoA

now real code

